import type { NextConfig } from "next";

const nextConfig: NextConfig = {
    /* config options here */
    images: {
      domains: ['127.0.0.1'], // Agrega aquí el dominio donde se aloja la imagen
    },
};

export default nextConfig;
